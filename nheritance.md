What is Inheritance?
=> Inheritance in Java is a mechanism where a subclass acquires the non-private members of a superclass, allowing code reuse, extension of behavior, and polymorphism. It models an IS-A relationship and enables runtime method overriding, but constructors are not inherited and Java supports only single inheritance for classes.
